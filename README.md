# Mini-HDFS-Simulation-Exploring-Distributed-Data-Storage

A simplified simulation of **HDFS (Hadoop Distributed File System)** implemented in Python.  
This project demonstrates the working principles of distributed storage systems — including **block replication**, **heartbeat monitoring**, **metadata management**, and **client interaction** through a **Flask web dashboard**.

---

## 🚀 Project Overview

This system simulates how Hadoop’s HDFS stores and manages large files across multiple DataNodes with a single NameNode controlling the metadata and coordination.

Each file uploaded through the client dashboard is:
1. Split into **2 MB chunks**
2. Replicated across multiple DataNodes
3. Managed by the NameNode’s metadata table
4. Monitored via heartbeats to detect DataNode failures
5. Downloaded by reconstructing all its chunks

---

## ⚙️ Architecture

### Components:
| Component | Description |
|------------|--------------|
| **NameNode** | Central coordinator — maintains metadata, upload/download plans, and monitors DataNodes via heartbeats. |
| **DataNode 0 & DataNode 1** | Store actual file chunks and send periodic heartbeats to the NameNode. |
| **Client** | Provides a Flask-based web interface for uploading, downloading, and monitoring system health. |
| **Config File (`config.json`)** | Defines IPs, ports, directories, and system parameters like replication factor and heartbeat interval. |

### System Flow:

Client → NameNode → DataNodes → NameNode → Client


1. Client uploads file → NameNode divides it into chunks and assigns DataNodes.
2. DataNodes store chunks and send heartbeat signals.
3. NameNode monitors DataNodes and maintains file metadata.
4. Client dashboard displays node health, metadata, and file distribution.

---

## 🏗️ Project Structure

📦 mini-hdfs-simulation/
│
├── namenode.py # Metadata management + heartbeat listener + replication logic
├── datanode0.py # DataNode 0 (chunk storage + heartbeat sender)
├── datanode1.py # DataNode 1 (chunk storage + heartbeat sender)
├── client.py # Flask web dashboard + file upload/download
├── config.json # Configuration for all components
├── metadata.json # Persistent metadata generated by the NameNode
├── requirements.txt # Python dependencies
└── README.md # This file

---

## ⚙️ Configuration File (`config.json`)

Example:
```json
{
    "namenode": {
        "host": "192.168.191.47",
        "client_port": 5000,
        "heartbeat_port": 5001
    },
    "datanodes": {
        "dn0": {
            "host": "192.168.191.86",
            "port": 6001,
            "storage_dir": "/home/pes2ug23cs485/bd-mini-proj/hdfs_storage/dn0"
        },
        "dn1": {
            "host": "192.168.160.129",
            "port": 6002,
            "storage_dir": "/home/pes2ug23cs484/bd-mini-proj/hdfs_storage/dn1"
        }
    },
    "replication_factor": 2,
    "chunk_size_mb": 2,
    "heartbeat_interval_sec": 3,
    "heartbeat_timeout_sec": 10
}

